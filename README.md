# Conferenze.Tech

## Generazione dei Comuni

Per poter generare i comuni, è necessario eseguire il seguente comando:

```bash
php artisan comuni:update-json
```

Questo genera il file `database/data/comuni.json` con {name, province_short_name}
